apiVersion: v1
kind: PersistentVolume
metadata:
  name: harbor-dev-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual-dev
  hostPath:
    path: /data/harbor-dev
    type: DirectoryOrCreate