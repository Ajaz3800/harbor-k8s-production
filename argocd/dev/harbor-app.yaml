apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: harbor-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://helm.goharbor.io
    chart: harbor
    targetRevision: "*"
    helm:
      valueFiles:
        - harbor/dev/values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: harbor-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true